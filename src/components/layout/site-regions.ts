/**
 * Ids shared between the layout shell and the components that have to reach
 * outside their own subtree: the skip link targets the main region, and the
 * mobile menu marks everything else inert while it is open.
 */
export const SITE_CONTENT_ID = "site-content";
export const MAIN_CONTENT_ID = "main-content";
