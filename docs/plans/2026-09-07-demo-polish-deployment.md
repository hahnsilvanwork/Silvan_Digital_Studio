# Demo refinement and separate deployments

User requested another full refinement pass with emphasis on readability and BMW buttons, and separate Vercel deployments. Preserve existing design identity.

Completed: all Falkenried routes (45 × desktop/mobile), all café and salon routes (8 × desktop/mobile), Steiner full-page desktop/mobile/menu checks. Automated axe WCAG A/AA checks returned zero violations after corrections. Also verified focus, menus, forms, image loading, anchors and navigation. This is a bounded audit, not a claim of exhaustive accessibility certification.

BMW hero CTAs now have solid high-contrast surfaces; weak service/tips numerals corrected. Café and salon light/dark surface colours, small text, menu and form states corrected. Steiner control boundaries, focus, touch targets and text sizes corrected.

All four Vercel production URLs were verified without login, across the exported routes and internal link destinations. Portfolio screenshot files refreshed and demo URLs changed to independent deployments. Main project retains local exports as a fallback.
