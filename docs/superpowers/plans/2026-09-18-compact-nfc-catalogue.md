# Compact NFC catalogue

Owner requested fewer categories and an initial selection of three Google models plus Instagram, chip, menu and custom. Show seven existing models in that order. Keep the full inventory behind a collapsed native disclosure with five application groups: Google, Instagram, chips, menu, custom and other uses. Original model IDs and enquiry presets are retained; legacy WhatsApp/Tripadvisor category links map to the broader group.

Validated: production build, lint, typecheck; all existing unit tests passed after updating visibility/group assertions, with two new locale coverage tests for the seven models and complete inventory reachability. Eight enquiry handoff browser checks and 26 catalogue/3D checks passed in Chromium and mobile Safari. Mobile and desktop visually inspected. Local preview updated; no deployment in this turn.
